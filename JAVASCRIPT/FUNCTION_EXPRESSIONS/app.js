//conventionally , functions are declared and defined like this


//this is called a function statement
function add(x,y){
    return x+y;
}

    // you could also store functions in a variable, this is called a function expression
const add = function (x,y){
    return x + y;
}